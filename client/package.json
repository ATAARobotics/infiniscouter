{
  "name": "infiniscouter",
  "module": "src/index.tsx",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "preact": "^10.17.1",
    "preact-iso": "^2.3.1",
    "preact-render-to-string": "^6.2.1"
  },
  "scripts": {
    "dev": "bun build --watch src/index.tsx --outdir=dist",
    "build": "bun build --minify src/index.tsx --outdir=dist"
  }
}
